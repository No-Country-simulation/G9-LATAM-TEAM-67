<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <!-- Configuración principal de Spring Boot -->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>

        <!-- Versión estable y compatible con Java 17 -->
        <version>3.5.4</version>

        <relativePath/>
    </parent>

    <!-- Información del proyecto -->
    <groupId>com.g9-latam-team-67</groupId>
    <artifactId>backend</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>backend</name>
    <description>Backend Hackathon - Módulo de Autenticación</description>

    <!-- Versión de Java utilizada -->
    <properties>
        <java.version>17</java.version>
    </properties>

    <dependencies>
        <!-- API REST                                                  -->
        <!-- Permite crear controladores, endpoints HTTP y ejecutar    -->
        <!-- el servidor embebido (Tomcat).                            -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- ========================================================= -->
        <!-- Spring Security                                           -->
        <!-- Proporciona autenticación, autorización, filtros          -->
        <!-- de seguridad y protección de rutas.                       -->
        <!-- ========================================================= -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>

        <!-- Spring Data JPA                                           -->
        <!-- Facilita la comunicación con la base de datos utilizando  -->
        <!-- entidades y repositorios, evitando escribir SQL manual.   -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <!-- Bean Validation                                           -->
        <!-- Permite validar automáticamente los datos recibidos       -->
        <!-- desde el cliente mediante anotaciones como @NotBlank,     -->
        <!-- @Email, @Size, etc.                                       -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- JWT (JSON Web Token)                                      -->
        <!-- Librería utilizada para generar y validar los tokens       -->
        <!-- de autenticación que utilizarán los usuarios.             -->
        <dependency>
            <groupId>com.auth0</groupId>
            <artifactId>java-jwt</artifactId>
            <version>4.5.0</version>
        </dependency>

        <!-- Flyway                                                    -->
        <!-- Permite versionar la base de datos mediante scripts SQL.  -->
        <!-- Cada cambio en la BD queda registrado mediante            -->
        <!-- migraciones.                                              -->
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>

        <!-- H2 Database                                               -->
        <!-- Base de datos en memoria para desarrollo y pruebas.       -->
        <!-- Más adelante podrá reemplazarse por Oracle, MySQL o       -->
        <!-- cualquier otra BD sin modificar la lógica del proyecto.   -->
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>runtime</scope>
        </dependency>


        <!-- Lombok                                                    -->
        <!-- Genera automáticamente getters, setters, constructores,  -->
        <!-- builder, equals, hashCode y otros métodos repetitivos.    -->
        <!-- Reduce considerablemente la cantidad de código escrito.   -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- Swagger / OpenAPI                                         -->
        <!-- Genera automáticamente la documentación de la API REST.   -->
        <!-- También permite probar los endpoints desde el navegador.  -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>2.8.5</version>
        </dependency>

        <!-- Testing                                                   -->
        <!-- Dependencias para realizar pruebas unitarias e            -->
        <!-- integrales utilizando JUnit, Mockito y Spring Test.       -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- ORACLE                                                  -->
        <dependency>
            <groupId>com.oracle.database.jdbc</groupId>
            <artifactId>ojdbc11</artifactId>
            <version>23.4.0.24.05</version>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-oracle</artifactId>
        </dependency>
        <dependency>
            <groupId>com.oracle.database.security</groupId>
            <artifactId>oraclepki</artifactId>
            <version>23.4.0.24.05</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-devtools</artifactId>
            <scope>runtime</scope>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>

        <!--        Manejo de variables de entorno-->
        <dependency>
            <groupId>me.paulschwarz</groupId>
            <artifactId>spring-dotenv</artifactId>
            <version>4.0.0</version>
        </dependency>
    </dependencies>

    <build>

        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>

        </plugins>

    </build>

</project>
